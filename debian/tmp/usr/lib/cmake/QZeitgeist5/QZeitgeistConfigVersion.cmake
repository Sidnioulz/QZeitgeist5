set(PACKAGE_VERSION 0.8.0)

if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL 0)
    set(PACKAGE_VERSION_COMPATIBLE 1)
    if("${PACKAGE_FIND_VERSION_MINOR}" EQUAL 8)
        set(PACKAGE_VERSION_EXACT 1)
    endif()
endif()

if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL 0 AND "${PACKAGE_FIND_VERSION_MINOR}" EQUAL 7)
    set(PACKAGE_VERSION_COMPATIBLE 0)
endif()
